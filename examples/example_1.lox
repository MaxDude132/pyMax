-*
class IteratorTest {
    init: List values = List() {
        self.values = values
    }

    add: IteratorTest other {
        self.values.extend(other.values)
    }

    iterate {
        return self.values.iterate()
    }
}

list = List()
list.extend(List(1, 2))
print(list)

test = IteratorTest(List(1))
test2 = IteratorTest(List(2, 3))
test3 = test + test2
print(test3.values)

for item in test3 {
    print(item)
}
*-