class IteratorTest {
    init: list values {
        self.values = values
    }

    iterate {
        return self.values.iterate()
    }
}

lst = list(1, 2, 3)
test = IteratorTest(lst)

for item in test {
    print item
}
